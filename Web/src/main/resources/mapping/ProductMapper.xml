<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bean.dao.ProductMapper">
  <resultMap id="BaseResultMap" type="com.bean.model.Product">
    <id column="pid" jdbcType="INTEGER" property="pid" />
    <result column="picurl" jdbcType="VARCHAR" property="picurl" />
    <result column="stockid" jdbcType="INTEGER" property="stockid" />
  </resultMap>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select pid, picurl, stockid
    from product
    where pid = #{pid,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select pid, picurl, stockid
    from product
  </select>
  <sql id="TableName">
    product
  </sql>
  <sql id="Values">
     #{pid,jdbcType=INTEGER} , 
     #{picurl,jdbcType=VARCHAR} , 
     #{stockid,jdbcType=INTEGER}
  </sql>
  <sql id="Columns">
    PID , 
    PICURL , 
    STOCKID
  </sql>
  <sql id="Criteria">
    <if test="pid != null">
      PID = #{pid,jdbcType=INTEGER} and 
    </if>
    <if test="picurl != null">
      PICURL = #{picurl,jdbcType=VARCHAR} and 
    </if>
    <if test="stockid != null">
      STOCKID = #{stockid,jdbcType=INTEGER}
    </if>
     and 1=1
  </sql>
  <sql id="UpdateCriteria">
    <if test="pid != null">
      PID = #{pid,jdbcType=INTEGER} , 
    </if>
    <if test="picurl != null">
      PICURL = #{picurl,jdbcType=VARCHAR} , 
    </if>
    <if test="stockid != null">
      STOCKID = #{stockid,jdbcType=INTEGER}
    </if>
     , 1=1
  </sql>
  <select id="selectByCriteria" parameterType="com.bean.model.Product" resultMap="ResultMapWithBLOBs">
    select <include refid="Columns"/> from <include refid="TableName"/> where <include refid="Criteria"/> 
  </select>
  <delete id="deleteByPrimaryKey" parameterType="Integer">
    delete from <include refid="TableName"/> where pid=#{pid}
  </delete>
  <delete id="deleteByCriteria" parameterType="com.bean.model.Product">
    delete from <include refid="TableName"/> where <include refid="Criteria"/>
  </delete>
  <insert id="insertByCriteria" parameterType="com.bean.model.Product">
    insert into <include refid="TableName"/>(<include refid="Columns"/>) values(<include refid="Values"/>)
  </insert>
  <update id="updateByCriteria" parameterType="com.bean.model.Product">
    update <include refid="TableName"/> set <include refid="UpdateCriteria"/> where <include refid="Criteria"/>
  </update>
</mapper>